celery
redis
numpy
opencv-python-headless
# Pin these exactly like context-engine to ensure stability
mediapipe==0.10.9
protobuf>=3.20.0,<5.0.0
# Note: kafka-python is likely not needed here anymore (since we use Redis), 
# but keeping it won't break anything.