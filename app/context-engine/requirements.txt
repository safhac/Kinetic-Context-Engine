kafka-python
pydantic
numpy
opencv-python-headless
# Pin MediaPipe to a stable version known to work with Python 3.9
mediapipe==0.10.9 
# Allow protobuf to find its own compatible version (do not hard pin to 3.20.3 unless necessary)
protobuf>=3.20.0,<5.0.0